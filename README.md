# project-catwalk-related-service

### Docker-compose:

- Run: `docker-compose up --renew-anon-volumes`
- Close: `docker-compose down -v`

### Run Tests:

- Run: `pg-test start`
- Stop: `pg-test stop`
